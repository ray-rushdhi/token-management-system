import { TokenSortPipe } from './token-sort.pipe';

describe('TokenSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TokenSortPipe();
    expect(pipe).toBeTruthy();
  });
});
